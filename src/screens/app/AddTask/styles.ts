import { StyleSheet } from 'react-native';
const style = StyleSheet.create({
    container: {
        flex: 1,
    },
    backContainer: {
        padding: 24,
    },
    icon: {
        width: 25,
        height: 25,
    },
});

export default style;
